#  我的爬蟲專案

這是一個用 `Python` 和 `Selenium` 撰寫的爬蟲，抓取政府財政數據並存成 CSV。

## 功能
自動控制瀏覽器  
選擇年份與數據類型  
解析 HTML，提取有用的資訊  
轉存成 CSV 格式  

## 安裝方式
請確保你已經安裝了 `Python` 和 `pip`，然後執行以下指令：

```bash
git clone https://github.com/你的帳號/你的倉庫.git
cd 你的倉庫
pip install -r requirements.txt


